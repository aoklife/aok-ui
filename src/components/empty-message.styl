@import '../colors'
@import '../mixins/typography'

.empty-message
  align-items center
  border 1px dashed $lightgray
  crisp-text()
  display flex
  flex-direction column
  font-body(18px)
  height 480px
  justify-content center
  text-align center

  > .img
    margin-bottom 2em

  > .text
    color $gray

  > .buttons
    margin 1em 0
