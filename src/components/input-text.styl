/*
InputText

.-error - has errors
.-prefix - has prefix
.-active - active


Markup:
<div class="input-text {{modifier_class}}">
  <input type="text" class="input"  name="">
  <div class="placeholder">Search</div>
</div>

Style guide: components.input-text
*/

/*
InputText with icon

.-error - has errors
.-prefix - has prefix
.-active - active


Markup:
<div class="input-text {{modifier_class}}">
  <div class="icon-prefix ion-icon ion-ios-email-outline"></div>
  <input type="text" class="input"  name="">
  <div class="placeholder">Search</div>
</div>

Style guide: components.input-text.1
*/

@import '../colors'
@import '../easing'
@import '../vars'
@import '../mixins/typography'

$input-height = 48px
$icon-width = 24px
$padding-top = 14px

input-field()
  appearance none
  background transparent
  border 0 solid transparent
  border-bottom 1px solid $gray
  border-radius 0
  box-sizing border-box
  crisp-text()
  font-bold()
  height $input-height
  line-height $input-height
  outline 0
  padding 0
  position relative
  width 100%
  max-width 100%
  z-index 2

  &:focus
    border-bottom-color $black

input-error()
  color $red
  > .input
  > .input:focus
    border-bottom-color $red
    color $red

  > .info
    color $red

input-placeholder()
  color $gray
  height $input-height
  left 0
  line-height $input-height
  pointer-events none
  position absolute
  top $padding-top
  transform translateY(0)
  transform-origin 0 0
  transition 450ms $easeOutCirc
  z-index 1

input-range-slider-thumb($size)
  appearance none
  background $black
  border-radius $size
  height $size
  width $size

input-range()
  > .input
    $size = 24px
    height 0
    margin unit($input-height/2, px) 0

    &::-ms-thumb
      input-range-slider-thumb($size)

    &::-moz-slider-thumb
      input-range-slider-thumb($size)

    &::-webkit-slider-thumb
      input-range-slider-thumb($size)

input-textarea()
  height auto

  > .input
    box-sizing border-box
    height auto
    line-height 1.5
    min-height $input-height
    padding-top 1em

icon-prefix()
  align-items center
  display flex
  height $input-height
  justify-content center
  left 0
  position absolute
  top $padding-top
  width $icon-width

  ~ .input
    padding-left $icon-width

  ~ .placeholder
    left $icon-width

.input-text
  crisp-text()
  font-body()
  padding-top $padding-top
  position relative

  ~ .input-text
  ~ .select-list
    margin-top 1em

  &.-range
    input-range()

  &.-error
    input-error()

  &.-textarea
    input-textarea()

  &.-disabled
    opacity 0.5
    > .input
      border-bottom-style dotted

  > .input
    input-field()

  > .placeholder
    input-placeholder()

  > .icon-prefix
    icon-prefix()

  > .info
    color rgba($black, 0.5)
    font-size 12px
    padding 0.5em 0

.input-text.-active:not(.-simple) > .placeholder
  font-size 14px
  left 0
  top 0
  transform scale(0.9) translateY(-30%)

.input-text.-simple.-active
  > .placeholder
    opacity 0
